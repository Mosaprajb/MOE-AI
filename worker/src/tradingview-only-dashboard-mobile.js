import { scannerOnlyHtml } from './tradingview-only-dashboard.js';

export { scannerOnlyHtml };

const CSS = `
:root{color-scheme:dark;--bg:#060b16;--surface:#0d1727;--surface2:#111f33;--surface3:#16283f;--line:#223b59;--text:#f7fbff;--muted:#8fa7be;--accent:#21d4c4;--accent2:#8b5cf6;--green:#38e19a;--red:#ff647c;--amber:#ffbd59;--blue:#54a7ff;--shadow:0 18px 60px rgba(0,0,0,.28)}
*{box-sizing:border-box;-webkit-tap-highlight-color:transparent}html{background:var(--bg)}body{margin:0;min-height:100vh;background:radial-gradient(circle at 15% -5%,rgba(33,212,196,.16),transparent 35%),radial-gradient(circle at 100% 0,rgba(139,92,246,.14),transparent 28%),var(--bg);color:var(--text);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;font-size:15px}.app{max-width:1180px;margin:auto;padding:14px 14px 110px}.topbar{position:sticky;top:0;z-index:12;margin:0 -14px 16px;padding:12px 14px;background:linear-gradient(180deg,rgba(6,11,22,.97),rgba(6,11,22,.86));backdrop-filter:blur(16px);border-bottom:1px solid rgba(255,255,255,.06)}.toprow{display:flex;align-items:center;justify-content:space-between;gap:12px}.brand{font-weight:950;font-size:21px;letter-spacing:-.4px}.subtitle{color:var(--muted);font-size:12px;margin-top:2px}.topactions{display:flex;align-items:center;gap:8px}.iconbtn,.button,.navbtn,.linkbtn{border:1px solid var(--line);background:var(--surface2);color:var(--text);border-radius:14px;min-height:44px;padding:10px 14px;font-weight:850;cursor:pointer;text-decoration:none}.iconbtn{width:46px;padding:0;display:grid;place-items:center;font-size:20px}.iconbtn.busy{animation:spin .8s linear infinite}.button.primary,.navbtn.active{background:linear-gradient(135deg,var(--accent),#34e2a8);color:#03211e;border-color:transparent}.button.danger{background:linear-gradient(135deg,var(--red),#ff8a63);color:#2a050b;border-color:transparent}.button.warn{background:linear-gradient(135deg,var(--amber),#ffdf7b);color:#2a1900;border-color:transparent}.button.small{min-height:38px;padding:8px 11px;font-size:12px}.statuspill{display:flex;align-items:center;gap:8px;border:1px solid var(--line);background:var(--surface2);border-radius:999px;padding:9px 12px;font-size:12px;font-weight:850;white-space:nowrap}.dot{width:10px;height:10px;border-radius:50%;background:var(--red);box-shadow:0 0 0 4px rgba(255,100,124,.12)}.dot.green{background:var(--green);box-shadow:0 0 0 4px rgba(56,225,154,.12);animation:pulse 1.35s ease-in-out infinite}.dot.amber{background:var(--amber);box-shadow:0 0 0 4px rgba(255,189,89,.12);animation:pulse 1.8s ease-in-out infinite}.desktopnav{display:flex;gap:8px;margin-top:12px;overflow:auto}.navbtn{white-space:nowrap;min-height:40px;padding:8px 13px}.view{display:none}.view.active{display:block}.grid{display:grid;grid-template-columns:repeat(12,minmax(0,1fr));gap:12px}.span12{grid-column:span 12}.span8{grid-column:span 8}.span6{grid-column:span 6}.span4{grid-column:span 4}.card,.panel{background:linear-gradient(180deg,rgba(255,255,255,.025),transparent),var(--surface);border:1px solid var(--line);border-radius:22px;box-shadow:var(--shadow)}.card{padding:16px}.hero{padding:18px;background:linear-gradient(145deg,rgba(33,212,196,.11),rgba(139,92,246,.08)),var(--surface);border:1px solid var(--line);border-radius:24px;margin-bottom:12px}.herohead{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.hero h1{font-size:21px;line-height:1.15;margin:0}.hero p{margin:7px 0 0;color:var(--muted);line-height:1.55}.metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px;margin-top:15px}.metric{padding:12px;border-radius:16px;background:var(--surface2);border:1px solid rgba(255,255,255,.07);min-width:0}.metric label{display:block;color:var(--muted);font-size:11px}.metric strong{display:block;margin-top:5px;font-size:15px;overflow-wrap:anywhere}.good{color:var(--green)!important}.bad{color:var(--red)!important}.warntext{color:var(--amber)!important}.muted{color:var(--muted)}.sectiontitle{font-size:16px;font-weight:900;margin:0 0 12px}.accounthead,.rowbetween{display:flex;align-items:center;justify-content:space-between;gap:10px}.connection{display:flex;align-items:center;gap:8px;font-size:12px;font-weight:850}.accountgrid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-top:12px}.accountstat{padding:11px;background:var(--surface2);border:1px solid rgba(255,255,255,.06);border-radius:15px}.accountstat small{display:block;color:var(--muted)}.accountstat b{display:block;margin-top:5px}.panel{overflow:hidden}.panel summary{list-style:none;display:flex;align-items:center;justify-content:space-between;gap:12px;padding:15px 16px;font-weight:900;cursor:pointer}.panel summary::-webkit-details-marker{display:none}.panel summary:after{content:"＋";font-size:20px;color:var(--accent)}.panel[open] summary:after{content:"−"}.panelbody{border-top:1px solid var(--line);padding:15px}.formgrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:11px}.field{display:grid;gap:6px}.field span{color:var(--muted);font-size:12px}.field input,.field select,.filter{width:100%;min-height:48px;border:1px solid var(--line);background:var(--surface2);color:var(--text);border-radius:14px;padding:10px 12px;font-size:15px}.switchline{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:13px;border-radius:16px;background:var(--surface2);border:1px solid var(--line);margin-top:11px}.switchline input{width:24px;height:24px;accent-color:var(--accent)}.actions{display:flex;flex-wrap:wrap;gap:9px;margin-top:13px}.message{min-height:18px;color:var(--muted);font-size:12px;margin-top:8px}.positionlist{display:grid;gap:12px}.positioncard{background:linear-gradient(145deg,rgba(33,212,196,.07),transparent 50%),var(--surface);border:1px solid var(--line);border-radius:22px;padding:16px}.positiontop{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.ticker{font-size:24px;font-weight:950;letter-spacing:.4px}.badge{display:inline-flex;align-items:center;border-radius:999px;padding:6px 9px;background:var(--surface3);border:1px solid var(--line);font-size:10px;font-weight:900}.badge.good{background:rgba(56,225,154,.1)}.badge.bad{background:rgba(255,100,124,.1)}.badge.warntext{background:rgba(255,189,89,.1)}.pnl{font-size:22px;font-weight:950;text-align:right}.posmetrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-top:14px}.posmetric{background:var(--surface2);border:1px solid rgba(255,255,255,.06);padding:10px;border-radius:14px}.posmetric small{display:block;color:var(--muted);font-size:10px}.posmetric b{display:block;margin-top:4px}.healthline{margin-top:12px;padding:11px;border-radius:14px;background:var(--surface2);border:1px solid var(--line);display:flex;align-items:flex-start;gap:9px}.healthline .dot{margin-top:4px}.chart{height:150px;margin-top:13px;border-radius:16px;background:#050c17;border:1px solid var(--line);overflow:hidden}.chart svg{width:100%;height:100%;display:block}.empty{padding:34px 15px;text-align:center;color:var(--muted);border:1px dashed var(--line);border-radius:18px}.eventlist,.archivelist{display:grid;gap:9px}.event,.archiveitem{padding:12px;border-radius:16px;background:var(--surface2);border:1px solid rgba(255,255,255,.07)}.eventtop,.archivehead{display:flex;justify-content:space-between;gap:10px}.eventtype{font-weight:900;font-size:12px;overflow-wrap:anywhere}.eventtime{color:var(--muted);font-size:10px;white-space:nowrap}.eventdetails{color:var(--muted);font-size:11px;line-height:1.5;margin-top:6px;overflow-wrap:anywhere}.archivegrid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:7px;margin-top:10px}.archivegrid span{padding:8px;border-radius:11px;background:rgba(255,255,255,.035);font-size:11px}.filters{display:grid;grid-template-columns:2fr 1fr 1fr auto;gap:8px;margin-bottom:12px}.bottomnav{display:none;position:fixed;z-index:15;bottom:0;left:0;right:0;padding:8px 8px calc(8px + env(safe-area-inset-bottom));background:rgba(6,11,22,.94);backdrop-filter:blur(18px);border-top:1px solid var(--line);grid-template-columns:repeat(5,1fr);gap:5px}.bottomnav button{border:0;background:transparent;color:var(--muted);padding:8px 3px;font-size:10px;font-weight:850}.bottomnav button b{display:block;font-size:19px;margin-bottom:2px}.bottomnav button.active{color:var(--accent)}.login{position:fixed;inset:0;z-index:50;background:rgba(2,6,13,.93);display:grid;place-items:center;padding:18px}.login[hidden]{display:none}.loginbox{width:min(420px,100%);background:var(--surface);border:1px solid var(--line);border-radius:24px;padding:22px;box-shadow:var(--shadow)}.loginbox h2{margin:0}.loginbox input{width:100%;min-height:52px;border:1px solid var(--line);background:var(--surface2);color:var(--text);border-radius:14px;padding:12px;margin-top:14px}.loginbox button{width:100%;margin-top:10px}.toast{position:fixed;z-index:60;left:50%;bottom:92px;transform:translate(-50%,20px);max-width:min(520px,calc(100% - 28px));padding:12px 15px;border:1px solid var(--line);background:var(--surface3);border-radius:15px;opacity:0;pointer-events:none;transition:.22s}.toast.show{opacity:1;transform:translate(-50%,0)}.loadingbar{height:2px;position:fixed;top:0;left:0;z-index:100;background:linear-gradient(90deg,var(--accent),var(--accent2));width:0;transition:width .2s}.loadingbar.on{width:72%;animation:loading 1.1s ease-in-out infinite}.dangerzone{border-color:rgba(255,100,124,.35)}.dangerzone summary{color:var(--red)}.compactnote{padding:11px;border-radius:14px;background:rgba(255,189,89,.08);border:1px solid rgba(255,189,89,.25);color:#ffdca3;font-size:12px;line-height:1.5}.sr{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0,0,0,0)}
@keyframes pulse{0%,100%{transform:scale(.86);opacity:.7}50%{transform:scale(1.18);opacity:1}}@keyframes spin{to{transform:rotate(360deg)}}@keyframes loading{0%{width:12%}50%{width:78%}100%{width:100%}}
@media(max-width:900px){.span8,.span6,.span4{grid-column:span 12}.desktopnav{display:none}.bottomnav{display:grid}.app{padding-bottom:100px}.metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.accountgrid{grid-template-columns:repeat(2,minmax(0,1fr))}.posmetrics{grid-template-columns:repeat(2,minmax(0,1fr))}.filters{grid-template-columns:1fr 1fr}.filters .wide{grid-column:span 2}}
@media(max-width:560px){.app{padding-left:10px;padding-right:10px}.topbar{margin-left:-10px;margin-right:-10px;padding-left:10px;padding-right:10px}.brand{font-size:18px}.subtitle{font-size:10px}.statuspill{max-width:155px;overflow:hidden;text-overflow:ellipsis}.hero{padding:15px}.hero h1{font-size:19px}.card,.positioncard{padding:14px;border-radius:19px}.formgrid{grid-template-columns:1fr}.archivegrid{grid-template-columns:repeat(2,minmax(0,1fr))}.positiontop{align-items:center}.ticker{font-size:21px}.pnl{font-size:19px}.button{width:100%}.actions .button.small{width:auto;flex:1}.topactions .iconbtn{width:44px}.metrics{gap:7px}.metric{padding:10px}.metric strong{font-size:14px}}
`;

function mobileClient() {
  const $ = (id) => document.getElementById(id);
  const money = (value) => Number.isFinite(Number(value)) ? `$${Number(value).toFixed(2)}` : '—';
  const escapeHtml = (value) => String(value ?? '').replace(/[&<>"']/g, (character) => ({
    '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;',
  }[character]));
  const appState = { data: null, fetching: false, signatures: Object.create(null), activeView: 'alerts', toastTimer: null };

  function setText(id, value, className = null) {
    const element = $(id);
    if (!element) return;
    const text = String(value ?? '');
    if (element.textContent !== text) element.textContent = text;
    if (className != null && element.className !== className) element.className = className;
  }
  function toast(message) {
    const element = $('toast');
    element.textContent = message;
    element.classList.add('show');
    clearTimeout(appState.toastTimer);
    appState.toastTimer = setTimeout(() => element.classList.remove('show'), 3800);
  }
  async function api(path, options = {}) {
    const response = await fetch(path, { cache: 'no-store', credentials: 'same-origin', ...options, headers: { 'content-type': 'application/json', 'x-moe-mobile-client': '1', ...(options.headers || {}) } });
    const data = await response.json().catch(() => ({}));
    if (response.status === 401) { $('login').hidden = false; throw new Error(data.error || 'Authentication required'); }
    if (!response.ok || data.ok === false) throw new Error(data.error || `HTTP ${response.status}`);
    return data;
  }
  function setView(name) {
    appState.activeView = name;
    document.querySelectorAll('.view').forEach((view) => view.classList.toggle('active', view.dataset.view === name));
    document.querySelectorAll('[data-nav]').forEach((button) => button.classList.toggle('active', button.dataset.nav === name));
    window.scrollTo({ top: 0, behavior: 'smooth' });
  }
  function connectionState(data) {
    const reception = data?.runtime?.receptionEnabled === true;
    const last = Date.parse(String(data?.runtime?.lastValidAlertAt || ''));
    const recent = Number.isFinite(last) && Date.now() - last <= 30 * 60_000;
    if (!reception) return { label: 'Reception off', color: '', className: 'bad' };
    if (recent) return { label: 'Connected · alert received', color: 'green', className: 'good' };
    return { label: 'Armed · waiting for alert', color: 'amber', className: 'warntext' };
  }
  function duration(seconds) {
    const total = Math.max(0, Math.floor(Number(seconds) || 0));
    const hours = Math.floor(total / 3600);
    const minutes = Math.floor((total % 3600) / 60);
    const secs = total % 60;
    return `${String(hours).padStart(2, '0')}:${String(minutes).padStart(2, '0')}:${String(secs).padStart(2, '0')}`;
  }
  function renderHeader(data) {
    const status = connectionState(data);
    $('statusDot').className = `dot ${status.color}`;
    setText('statusLabel', status.label);
    setText('receptionMetric', data.runtime?.receptionEnabled ? 'ON' : 'OFF', data.runtime?.receptionEnabled ? 'good' : 'bad');
    setText('lastAlertMetric', data.runtime?.lastValidAlertAt ? new Date(data.runtime.lastValidAlertAt).toLocaleString() : 'Never');
    setText('accountMetric', data.runtime?.accountType || 'DEMO');
    const clock = data.marketClock || {};
    setText('sessionMetric', `${clock.label || 'Closed'} · ${clock.phase || 'CLOSED'}`, clock.entryAllowed ? 'good' : 'bad');
    const next = Date.parse(String(clock.nextTransitionAt || ''));
    setText('sessionRemainingMetric', Number.isFinite(next) ? duration(Math.max(0, Math.ceil((next - Date.now()) / 1000))) : '—');
    const flatten = Date.parse(String(clock.autoFlattenAt || ''));
    setText('flattenMetric', Number.isFinite(flatten) ? duration(Math.max(0, Math.ceil((flatten - Date.now()) / 1000))) : '—', clock.autoFlattenDue ? 'bad' : 'good');
    setText('entryMetric', clock.entryAllowed ? `OPEN · ${clock.selectedSession || 'ALL'}` : `BLOCKED · ${clock.entryBlockedReason || 'CLOSED'}`, clock.entryAllowed ? 'good' : 'bad');
  }
  function renderAccount(prefix, account = {}) {
    setText(`${prefix}Balance`, money(account.balance));
    setText(`${prefix}Power`, money(account.buyingPower));
    setText(`${prefix}Open`, account.openPositions || 0);
    setText(`${prefix}Pnl`, money(account.totalPnl || 0), Number(account.totalPnl || 0) >= 0 ? 'good' : 'bad');
    const connected = account.connected === true;
    $(`${prefix}Dot`).className = `dot ${connected ? 'green' : ''}`;
    setText(`${prefix}Connection`, connected ? 'Connected' : 'Disconnected');
    setText(`${prefix}ConnectionDetail`, account.locked ? 'Locked' : (account.error || ''));
  }
  function positionPnl(position) { return (Number(position.lastPrice || position.entryPrice) - Number(position.entryPrice)) * Number(position.quantity || 0); }
  function pathFor(points, key, width = 800, height = 180) {
    const values = points.flatMap((point) => [Number(point.price), Number(point.stop)]).filter(Number.isFinite);
    if (points.length < 2 || !values.length) return '';
    const low = Math.min(...values); const high = Math.max(...values); const range = Math.max(0.01, high - low);
    return points.map((point, index) => { const x = 18 + index * (width - 36) / Math.max(1, points.length - 1); const y = height - 16 - (Number(point[key]) - low) * (height - 32) / range; return `${index ? 'L' : 'M'}${x.toFixed(1)},${y.toFixed(1)}`; }).join(' ');
  }
  function protectionStatus(position) {
    if (position.lastError) return { label: 'Protection error', className: 'bad', dot: '' };
    if (position.status === 'CLOSING_KILL') return { label: 'Exit pending', className: 'warntext', dot: 'amber' };
    if (position.trailingActivated) return { label: `Trailing active · stop ${money(position.currentStopPrice)}`, className: 'good', dot: 'green' };
    if (position.orderIds?.currentStop) return { label: `Initial protection · stop ${money(position.currentStopPrice)}`, className: 'warntext', dot: 'amber' };
    return { label: 'Protection missing', className: 'bad', dot: '' };
  }
  function renderPositions(positions = []) {
    const visible = positions.filter((position) => position && position.symbol && position.positionOpen !== false);
    const signature = JSON.stringify(visible.map((position) => ({ s: position.symbol, st: position.status, e: position.entryPrice, p: position.lastPrice, h: position.highWaterPrice, x: position.currentStopPrice, q: position.quantity, t: position.trailingActivated, er: position.lastError, u: position.updatedAt, ph: (position.priceHistory || []).slice(-40) })));
    if (appState.signatures.positions === signature) return;
    appState.signatures.positions = signature;
    const containers = [$('positionsList'), $('homePositions')];
    if (!visible.length) { containers.forEach((container) => { container.innerHTML = '<div class="empty">No open positions.</div>'; }); setText('positionCountBadge', '0'); return; }
    setText('positionCountBadge', visible.length);
    const html = visible.map((position) => {
      const pnl = positionPnl(position); const health = protectionStatus(position); const points = Array.isArray(position.priceHistory) ? position.priceHistory.slice(-90) : []; const pricePath = pathFor(points, 'price'); const stopPath = pathFor(points, 'stop');
      return `<article class="positioncard"><div class="positiontop"><div><div class="ticker">${escapeHtml(position.symbol)}</div><div class="muted">${escapeHtml(position.status || 'OPEN')} · ${escapeHtml(position.accountType || 'DEMO')}</div></div><div><div class="pnl ${pnl >= 0 ? 'good' : 'bad'}">${money(pnl)}</div><div class="muted" style="text-align:right;font-size:11px">${Number(position.quantity || 0)} shares</div></div></div><div class="posmetrics"><div class="posmetric"><small>Entry</small><b>${money(position.entryPrice)}</b></div><div class="posmetric"><small>Current</small><b>${money(position.lastPrice)}</b></div><div class="posmetric"><small>High</small><b>${money(position.highWaterPrice)}</b></div><div class="posmetric"><small>Stop</small><b>${money(position.currentStopPrice)}</b></div><div class="posmetric"><small>Target</small><b>${money(position.takeProfitPrice)}</b></div><div class="posmetric"><small>Updated</small><b>${position.updatedAt ? new Date(position.updatedAt).toLocaleTimeString() : '—'}</b></div></div><div class="healthline"><span class="dot ${health.dot}"></span><div><b class="${health.className}">${health.label}</b><div class="muted" style="font-size:11px;margin-top:3px">${position.lastError ? escapeHtml(position.lastError) : 'Automatic monitoring and broker protection reconciliation are active.'}</div></div></div><div class="chart"><svg viewBox="0 0 800 180" preserveAspectRatio="none"><path d="${pricePath}" fill="none" stroke="#21d4c4" stroke-width="4" vector-effect="non-scaling-stroke"/><path d="${stopPath}" fill="none" stroke="#ff647c" stroke-width="4" vector-effect="non-scaling-stroke"/></svg></div><div class="actions"><button class="button small primary" data-repair="${escapeHtml(position.symbol)}">Refresh & repair protection</button><button class="button small danger" data-close="${escapeHtml(position.symbol)}">Close position</button></div></article>`;
    }).join('');
    containers.forEach((container) => { container.innerHTML = html; });
    document.querySelectorAll('[data-repair]').forEach((button) => button.addEventListener('click', () => repairPosition(button.dataset.repair)));
    document.querySelectorAll('[data-close]').forEach((button) => button.addEventListener('click', () => closePosition(button.dataset.close)));
  }
  const importantTypes = new Set(['TRADINGVIEW_ALERT_RECEIVED','TRADINGVIEW_ALERT_QUEUE_COMPLETED','TRADINGVIEW_ALERT_PROCESSING_FAILED','TRADINGVIEW_POSITION_SUBMITTED','TRADINGVIEW_POSITION_OPENED','TRAILING_STOP_PLACED','TRAILING_STOP_RAISED','TRAILING_STOP_ACTIVATED_BY_RECONCILIATION','TRAILING_STOP_CATCHUP_EXIT_REQUIRED','STOP_PROTECTION_REPAIRED','TRADINGVIEW_POSITION_CLOSED','TRADINGVIEW_BUY_BLOCKED','TRADINGVIEW_DUPLICATE_ALERT_IGNORED','PROTECTION_FAILURE_MARKET_EXIT_SUBMITTED','PROTECTION_FAILURE_MARKET_EXIT_FAILED']);
  function eventTone(type) { if (/FAILED|ERROR|BLOCKED|MISSING/i.test(type)) return 'bad'; if (/OPENED|COMPLETED|RAISED|PLACED|REPAIRED|RECEIVED/i.test(type)) return 'good'; return 'warntext'; }
  function eventSummary(event) { const type = String(event.type || 'EVENT'); const symbol = event.symbol ? ` · ${event.symbol}` : ''; const detail = event.error || event.reason || event.currentStopPrice || event.alertPrice || ''; return { title: `${type.replaceAll('_', ' ')}${symbol}`, detail: String(detail || '') }; }
  function renderEvents(audit = []) {
    const important = audit.filter((event) => importantTypes.has(String(event.type || ''))).slice(0, 60);
    const signature = JSON.stringify(important.map((event) => [event.id,event.type,event.createdAt,event.error,event.reason,event.currentStopPrice]));
    if (appState.signatures.events === signature) return; appState.signatures.events = signature;
    const html = important.length ? important.map((event) => { const summary = eventSummary(event); return `<div class="event"><div class="eventtop"><div class="eventtype ${eventTone(event.type)}">${escapeHtml(summary.title)}</div><div class="eventtime">${event.createdAt ? new Date(event.createdAt).toLocaleString() : ''}</div></div><div class="eventdetails">${escapeHtml(summary.detail || JSON.stringify(event).slice(0,220))}</div></div>`; }).join('') : '<div class="empty">No execution notifications yet.</div>';
    $('activityList').innerHTML = html; $('homeEvents').innerHTML = important.length ? important.slice(0,5).map((event) => { const summary = eventSummary(event); return `<div class="event"><div class="eventtop"><div class="eventtype ${eventTone(event.type)}">${escapeHtml(summary.title)}</div><div class="eventtime">${event.createdAt ? new Date(event.createdAt).toLocaleString() : ''}</div></div><div class="eventdetails">${escapeHtml(summary.detail)}</div></div>`; }).join('') : '<div class="empty">Waiting for the next TradingView event.</div>'; setText('notificationCountBadge', important.filter((event) => /FAILED|ERROR|BLOCKED/i.test(event.type || '')).length);
  }
  function renderArchive(archive = []) {
    const query = String($('archiveTicker').value || '').trim().toUpperCase(); const reason = $('archiveReason').value; const account = $('archiveAccount').value; const filtered = archive.filter((item) => (!query || String(item.symbol || '').includes(query)) && (!reason || item.exitReason === reason) && (!account || item.accountType === account));
    const signature = JSON.stringify(filtered.map((item) => [item.id,item.closedAt,item.profitLoss,item.exitReason])); if (appState.signatures.archive === signature) return; appState.signatures.archive = signature;
    $('archiveList').innerHTML = filtered.length ? filtered.map((item) => `<article class="archiveitem"><div class="archivehead"><b>${escapeHtml(item.symbol)}</b><b class="${Number(item.profitLoss) >= 0 ? 'good' : 'bad'}">${money(item.profitLoss)}</b></div><div class="archivegrid"><span>Entry ${money(item.entryPrice)}</span><span>Exit ${money(item.exitPrice)}</span><span>${escapeHtml(item.exitReason)}</span><span>${Math.round(Number(item.durationSeconds || 0)/60)} min</span><span>${escapeHtml(item.accountType)}</span><span>${item.closedAt ? new Date(item.closedAt).toLocaleString() : ''}</span></div></article>`).join('') : '<div class="empty">No matching closed trades.</div>';
  }
  function writeForm(settings = {}) {
    const map = { positionSize: settings.positionSizeDollars, takeProfit: settings.takeProfitDollars, stopLoss: settings.stopLossDollars, dailyLoss: settings.maxDailyLossDollars, maxPositions: settings.maxOpenPositions, accountType: settings.accountType || 'DEMO', tradingMode: settings.tradingMode || 'CASH_LONG_ONLY', buyingPowerPercent: settings.maxBuyingPowerPercent ?? 25, orderSession: settings.session || 'ALL', flattenTime: settings.autoFlattenTimeLocal || '18:55' };
    Object.entries(map).forEach(([id,value]) => { const element = $(id); if (element && document.activeElement !== element && element.dataset.dirty !== '1') element.value = value ?? ''; });
    if (document.activeElement !== $('trailingEnabled')) $('trailingEnabled').checked = settings.trailingEnabled !== false;
  }
  function apply(data) {
    appState.data = data; renderHeader(data); renderAccount('demo', data.accounts?.demo || {}); renderAccount('live', data.accounts?.live || {}); writeForm(data.settings || {}); $('receptionToggle').checked = data.runtime?.receptionEnabled === true; renderPositions(data.positions || []); renderEvents(data.audit || []); renderArchive(data.archive || []); setText('settingsVersion', `Schema ${data.settingsSchema || 'V2'} · ${data.generatedAt ? new Date(data.generatedAt).toLocaleTimeString() : ''}`); setText('killStatus', data.runtime?.killSwitchActive ? 'Kill switch ACTIVE' : 'Kill switch clear', data.runtime?.killSwitchActive ? 'bad' : 'good');
  }
  async function loadStatus(showToast = false) {
    if (appState.fetching) return; appState.fetching = true; $('loading').classList.add('on'); $('refreshButton').classList.add('busy');
    try { const data = await api('/api/tradingview/status'); apply(data); $('login').hidden = true; if (showToast) toast('Dashboard updated'); } catch (error) { if (showToast) toast(error.message); } finally { appState.fetching = false; $('loading').classList.remove('on'); $('refreshButton').classList.remove('busy'); }
  }
  async function forceRefresh() {
    if (appState.fetching) return; appState.fetching = true; $('loading').classList.add('on'); $('refreshButton').classList.add('busy');
    try { const result = await api('/api/tradingview/refresh', { method: 'POST', body: '{}' }); if (result.status) apply(result.status); toast('Prices, orders and protection refreshed'); } catch (error) { toast(error.message); } finally { appState.fetching = false; $('loading').classList.remove('on'); $('refreshButton').classList.remove('busy'); }
  }
  async function repairPosition(symbol) { if (!symbol) return; toast(`${symbol}: checking price history and protection…`); try { const result = await api('/api/tradingview/repair', { method: 'POST', body: JSON.stringify({ symbol }) }); if (result.status) apply(result.status); toast(`${symbol}: protection reconciliation completed`); } catch (error) { toast(`${symbol}: ${error.message}`); } }
  async function closePosition(symbol) { if (!symbol || !confirm(`Close the entire ${symbol} position now?`)) return; try { const result = await api('/api/tradingview/position/close', { method: 'POST', body: JSON.stringify({ symbol, confirmation: 'CLOSE' }) }); if (result.status) apply(result.status); toast(`${symbol}: exit submitted`); } catch (error) { toast(error.message); } }
  async function login() { const message = $('loginMessage'); message.textContent = ''; try { await api('/api/tradingview/session', { method: 'POST', body: JSON.stringify({ pin: $('pin').value }) }); $('pin').value = ''; $('login').hidden = true; await loadStatus(); } catch (error) { message.textContent = error.message; } }
  async function saveSettings() {
    const payload = { positionSizeDollars: Number($('positionSize').value), takeProfitDollars: Number($('takeProfit').value), stopLossDollars: Number($('stopLoss').value), maxDailyLossDollars: Number($('dailyLoss').value), maxOpenPositions: Number($('maxPositions').value), accountType: $('accountType').value, tradingMode: $('tradingMode').value, maxBuyingPowerPercent: Number($('buyingPowerPercent').value), session: $('orderSession').value, autoFlattenTimeLocal: $('flattenTime').value, autoFlattenTimezone: appState.data?.settings?.autoFlattenTimezone || 'America/Chicago', trailingEnabled: $('trailingEnabled').checked };
    try { await api('/api/tradingview/settings', { method: 'PUT', body: JSON.stringify(payload) }); document.querySelectorAll('[data-dirty="1"]').forEach((element) => { element.dataset.dirty = '0'; }); toast('Settings saved. Alert reception was turned off for safety.'); await loadStatus(); } catch (error) { toast(error.message); }
  }
  async function setReception(enabled) { try { const confirmation = enabled && $('accountType').value === 'LIVE' ? prompt('Type CONFIRM to activate Live for the first time') : ''; await api('/api/tradingview/reception', { method: 'POST', body: JSON.stringify({ enabled, accountType: $('accountType').value, confirmation }) }); toast(enabled ? 'TradingView reception enabled' : 'TradingView reception disabled'); await loadStatus(); } catch (error) { $('receptionToggle').checked = !enabled; toast(error.message); } }
  async function killSwitch() { if (!confirm('Emergency action: close all open positions and stop alert reception?')) return; try { await api('/api/tradingview/kill-switch', { method: 'POST', body: JSON.stringify({ action: 'ACTIVATE' }) }); toast('Emergency exits submitted'); await loadStatus(); } catch (error) { toast(error.message); } }
  async function clearKill() { if (!confirm('Clear the kill switch? Alert reception remains off until re-enabled.')) return; try { await api('/api/tradingview/kill-switch', { method: 'POST', body: JSON.stringify({ action: 'CLEAR', confirmation: 'CLEAR' }) }); toast('Kill switch cleared'); await loadStatus(); } catch (error) { toast(error.message); } }
  function bind() {
    document.querySelectorAll('[data-nav]').forEach((button) => button.addEventListener('click', () => setView(button.dataset.nav))); $('refreshButton').addEventListener('click', forceRefresh); $('loginButton').addEventListener('click', login); $('pin').addEventListener('keydown', (event) => { if (event.key === 'Enter') login(); }); $('saveSettings').addEventListener('click', saveSettings); $('receptionToggle').addEventListener('change', (event) => setReception(event.target.checked)); $('killButton').addEventListener('click', killSwitch); $('clearKillButton').addEventListener('click', clearKill); $('archiveExport').addEventListener('click', () => { location.href = '/api/tradingview/archive.csv'; }); ['archiveTicker','archiveReason','archiveAccount'].forEach((id) => $(id).addEventListener('input', () => { appState.signatures.archive = ''; renderArchive(appState.data?.archive || []); })); document.querySelectorAll('.field input,.field select').forEach((element) => element.addEventListener('input', () => { element.dataset.dirty = '1'; })); setInterval(() => { if (!document.hidden) loadStatus(); }, 5000); setInterval(() => { if (appState.data) renderHeader(appState.data); }, 500);
  }
  bind(); loadStatus();
}

export function tradingViewMobileDashboardHtml() {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#060b16"><title>MOE TradingView Control</title><style>${CSS}</style></head><body><div class="loadingbar" id="loading"></div><div class="login" id="login"><div class="loginbox"><h2>Secure TradingView control</h2><p class="muted">Enter the control PIN. Broker and webhook secrets never reach this browser.</p><input id="pin" type="password" inputmode="numeric" autocomplete="one-time-code" placeholder="Control PIN"><button id="loginButton" class="button primary">Unlock dashboard</button><div class="message" id="loginMessage"></div></div></div><div class="app"><header class="topbar"><div class="toprow"><div><div class="brand">MOE · TradingView</div><div class="subtitle">Long-only execution · whole-trade controls</div></div><div class="topactions"><div class="statuspill"><span class="dot" id="statusDot"></span><span id="statusLabel">Checking…</span></div><button class="iconbtn" id="refreshButton" aria-label="Refresh prices, orders and protection">↻</button></div></div><nav class="desktopnav"><button class="navbtn active" data-nav="alerts">Overview</button><button class="navbtn" data-nav="positions">Positions <span id="positionCountBadge">0</span></button><button class="navbtn" data-nav="archive">Archive</button><button class="navbtn" data-nav="activity">Activity <span id="notificationCountBadge">0</span></button><button class="navbtn" data-nav="settings">Settings</button><a class="linkbtn" href="/scanner">Scanner · research only</a></nav></header><main><section class="view active" data-view="alerts"><div class="hero"><div class="herohead"><div><h1>TradingView webhooks are the only entry source</h1><p>The scanner remains research-only. Every ticker has isolated execution and protection state.</p></div><span class="badge good">TRADINGVIEW ONLY</span></div><div class="metrics"><div class="metric"><label>Reception</label><strong id="receptionMetric">—</strong></div><div class="metric"><label>Last alert</label><strong id="lastAlertMetric">—</strong></div><div class="metric"><label>Account</label><strong id="accountMetric">—</strong></div><div class="metric"><label>Webhook</label><strong>/api/tradingview/signal</strong></div><div class="metric"><label>Market session</label><strong id="sessionMetric">—</strong></div><div class="metric"><label>Session remaining</label><strong id="sessionRemainingMetric">—</strong></div><div class="metric"><label>Entry window</label><strong id="entryMetric">—</strong></div><div class="metric"><label>Auto-flatten</label><strong id="flattenMetric">—</strong></div></div></div><div class="grid"><article class="card span6"><div class="accounthead"><h2 class="sectiontitle">Demo / Paper account</h2><div class="connection"><span class="dot" id="demoDot"></span><span id="demoConnection">—</span><span class="muted" id="demoConnectionDetail"></span></div></div><div class="accountgrid"><div class="accountstat"><small>Balance</small><b id="demoBalance">—</b></div><div class="accountstat"><small>Buying power</small><b id="demoPower">—</b></div><div class="accountstat"><small>Positions</small><b id="demoOpen">0</b></div><div class="accountstat"><small>Total P/L</small><b id="demoPnl">—</b></div></div></article><article class="card span6"><div class="accounthead"><h2 class="sectiontitle">Live account · locked</h2><div class="connection"><span class="dot" id="liveDot"></span><span id="liveConnection">—</span><span class="muted" id="liveConnectionDetail"></span></div></div><div class="accountgrid"><div class="accountstat"><small>Balance</small><b id="liveBalance">—</b></div><div class="accountstat"><small>Buying power</small><b id="livePower">—</b></div><div class="accountstat"><small>Positions</small><b id="liveOpen">0</b></div><div class="accountstat"><small>Total P/L</small><b id="livePnl">—</b></div></div></article><details class="panel span12" open><summary>Open positions</summary><div class="panelbody"><div class="positionlist" id="homePositions"><div class="empty">No open positions.</div></div></div></details><details class="panel span12"><summary>Recent execution notifications</summary><div class="panelbody"><div class="eventlist" id="homeEvents"></div></div></details></div></section><section class="view" data-view="positions"><div class="rowbetween" style="margin-bottom:12px"><div><h1 style="margin:0;font-size:22px">Open positions</h1><div class="muted">Live broker state, price history and protection health.</div></div><button class="button primary small" onclick="document.getElementById('refreshButton').click()">Refresh all</button></div><div class="positionlist" id="positionsList"><div class="empty">No open positions.</div></div></section><section class="view" data-view="archive"><div class="card"><div class="rowbetween"><div><h2 class="sectiontitle">Closed trade archive</h2><div class="muted">Searchable, filterable and exportable.</div></div><button class="button small" id="archiveExport">Export CSV</button></div><div class="filters"><input class="filter wide" id="archiveTicker" placeholder="Ticker"><select class="filter" id="archiveReason"><option value="">All reasons</option><option>TARGET</option><option>STOP_LOSS</option><option>TRAILING_STOP</option><option>KILL_SWITCH</option><option>AUTO_FLATTEN</option><option>BROKER_EXIT</option></select><select class="filter" id="archiveAccount"><option value="">All accounts</option><option>DEMO</option><option>LIVE</option></select></div><div class="archivelist" id="archiveList"></div></div></section><section class="view" data-view="activity"><div class="card"><h2 class="sectiontitle">Alerts, orders and protection events</h2><div class="muted" style="margin-bottom:12px">Failures and blocked actions are shown in red. Stop raises and successful actions are green.</div><div class="eventlist" id="activityList"></div></div></section><section class="view" data-view="settings"><div class="grid"><details class="panel span12" open><summary>Trade and risk settings</summary><div class="panelbody"><div class="formgrid"><label class="field"><span>Position size $</span><input id="positionSize" type="number" min="1" step="0.01"></label><label class="field"><span>Take profit total $</span><input id="takeProfit" type="number" min="0.01" step="0.01"></label><label class="field"><span>Stop loss total $</span><input id="stopLoss" type="number" min="0.01" step="0.01"></label><label class="field"><span>Daily max loss $</span><input id="dailyLoss" type="number" min="0.01" step="0.01"></label><label class="field"><span>Max open positions</span><input id="maxPositions" type="number" min="1" max="30"></label><label class="field"><span>Account</span><select id="accountType"><option value="DEMO">Demo / Paper</option><option value="LIVE">Live</option></select></label><label class="field"><span>Trading mode</span><select id="tradingMode"><option value="CASH_LONG_ONLY">Cash long only</option><option value="CASH_PLUS_MARGIN_LONG">Cash + margin long</option></select></label><label class="field"><span>Max buying power %</span><input id="buyingPowerPercent" type="number" min="1" max="100"></label><label class="field"><span>Order session</span><select id="orderSession"><option value="CORE">Regular only · CORE</option><option value="ALL">Pre + regular + after-hours · ALL</option></select></label><label class="field"><span>Auto-flatten local time</span><input id="flattenTime" type="time"></label></div><div class="switchline"><div><b>Manual trailing</b><div class="muted">At +2¢: stop to entry +2¢. Every additional +5¢: stop +1¢.</div></div><input id="trailingEnabled" type="checkbox" checked></div><div class="compactnote" style="margin-top:11px"><b>No overnight holding.</b> New overnight entries are blocked and all positions are flattened before the configured cutoff. Timezone: America/Chicago.</div><div class="actions"><button class="button primary" id="saveSettings">Save settings</button></div><div class="message" id="settingsVersion"></div></div></details><details class="panel span12" open><summary>TradingView reception</summary><div class="panelbody"><div class="switchline"><div><b>Receive TradingView alerts</b><div class="muted">When off, alerts are logged but no order is submitted.</div></div><input id="receptionToggle" type="checkbox"></div></div></details><details class="panel span12 dangerzone"><summary>Emergency controls</summary><div class="panelbody"><p class="muted">Stops alert ingestion, cancels tracked protection orders and submits exits for open positions.</p><div class="actions"><button class="button danger" id="killButton">ACTIVATE KILL SWITCH</button><button class="button" id="clearKillButton">Clear kill switch</button></div><div class="message" id="killStatus"></div></div></details><a class="card span12 linkbtn" href="/scanner">Open Scanner · research only</a></div></section></main></div><nav class="bottomnav"><button class="active" data-nav="alerts"><b>⌂</b>Overview</button><button data-nav="positions"><b>↗</b>Positions</button><button data-nav="archive"><b>▤</b>Archive</button><button data-nav="activity"><b>◉</b>Activity</button><button data-nav="settings"><b>⚙</b>Settings</button></nav><div class="toast" id="toast"></div><script>(${mobileClient.toString()})();</script></body></html>`;
}
